import random

numbers = [1, 2, 3, 4, 5, 6]

random.shuffle(numbers) #this shuffles the numbers list elements

print(numbers) #shuffled output

num = random.choice(numbers) #shuffles each element

print(num) # prints radom element from numbers list